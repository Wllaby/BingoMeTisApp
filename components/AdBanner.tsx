
// This file is intentionally empty and serves as a fallback
// The actual implementations are in AdBanner.native.tsx and AdBanner.web.tsx
// Metro bundler will automatically resolve to the correct platform-specific file

export { AdBanner } from './AdBanner.native';
